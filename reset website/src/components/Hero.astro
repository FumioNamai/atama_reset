---
import { Image } from "astro:assets"
import hero from "../assets/hero.webp"
---


<section class="hero">
  <div class="container">
    <Image src = { hero } alt="スッキリ爽快！" class = "heroImg" loading="eager" width="1000" height="605"/>
    <div class="horizon">
      <h1>
        <span class="subTitle">頭の整体で身体の不調を整える</span><br>
      頭ほぐし専門店 reset</h1>
    </div>
  </div>
</section>

<style>

  .heroImg {
    width: 100%;
    min-width: 375px;
    max-height: 605px;
    height: 70vh;
    object-fit: cover;
    opacity: 0;
    /* セクション全体をフェードインさせる */
    animation: fadein 3s forwards linear;
  }
  @keyframes fadein {
    0% {
      opacity: 0;
    }
    100% {
      opacity: 1;
    }
  }

  .container {
    width: 100%;
    height: 70vh;
    min-width: 375px;
    min-height: 500px;
    position: relative;
  }

  .horizon {
    height: clamp(100px,20vw ,175px);
    overflow: hidden;
    position: absolute;
    top: 55%;
    left: 50%;
    transform: translateX(-50%);
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  h1 {
    color: var(--textWhite);
    text-shadow:
    0px 0px 10px #000;
    /* 名前を下から浮き上がらせる */
    animation: rising 3s forwards linear;
    opacity: 0;
    line-height: 150%;
    text-align: center;
    font-size: clamp(3.4rem, 1rem + 5vw,5.5rem );
  }

  .subTitle {
    font-size: clamp(2rem, 1rem + 2vw,3rem );
  }
  @keyframes rising {
    0% {
      transform: translateY(clamp(100px,20vw ,175px));
      opacity: 0;
    }
    80% {
      opacity: 0;
    }
    100% {
      transform: translateY(0);
      opacity: 1;
    }
  }
</style>
