---
---

<section class="features">
  <div class="reason">
    <h3 class="fadein">選ばれる理由</h3>
    <div class="wrapper">
      <div class="grid_item one fadein">
        <h4>➀ 整体法を取り入れた施術</h4>
        <p>
          当店の頭ほぐしは、整体師監修のもと<br
          />整体法を取り入れたオールハンドの施術です。
        </p>
      </div>
      <div class="grid_item two fadein">
        <h4>➁ 解剖学に基づいた施術</h4>
        <p>
          脳の血流の80%を占めるとされる内頚動脈。外側からのマッサージで刺激するのは不可能です。<br
          />
          <br />
          しかし、当店では解剖学の視点からの手法により、内頸動脈を刺激し、血流を改善します。
        </p>
      </div>
      <div class="grid_item three fadein">
        <h4>➂ 科学的に根拠のある施術</h4>
        <p>
          当店で行うヘッドマッサージはセロトニン脳波測定の実験により、セロトニン神経が活性化することが証明されました。<br
          />
          <br /><span style="font-size: 16px;  letter-spacing: 0.1em;">
            ※セロトニン…自律神経を整えてくれる神経物質
          </span>
        </p>
        <button>詳しくはこちら</button>
      </div>
      <div class="grid_item img1  fadeinImg"></div>
      <div class="grid_item four  fadein">
        <h4>➃ ベッドを使っての施術</h4>
        <p>
          当店の頭ほぐしは「頭から全身を整える」マッサージです。<br />
          <br />
          全身を整えるためには、ベッドでの施術が必須です。<br />
          <br />
          ベッドを使って『頭蓋仙骨リズム』に有効な施術をおこない、全身を緩め、血流を良くしていきます。<br
          />
          <br />
          また、脳脊髄液の循環を良くすることで、老廃物の排出を促し、お身体の健康状態を維持します。<br
          />
          <br />
          頭の施術による首への負担を防ぐためにも、ベッドを使用します。
        </p>
      </div>
      <div class="grid_item five fadein">
        <h4>➄ タオルの上から揉みません</h4>
        <p>
          頭は非常に薄い筋肉で覆われているので、頭を強くも揉む（押す）ことはできません。<br
          />
          <br />
          頭に多く集中する大切なツボを適切な力で刺激するためにも、頭皮を直接しっかりと捉えてほぐします。<br
          />
          <br />
          また、タオルの上から髪の毛を摩擦すると髪の毛のキューティクルを傷つけてしまいます。<br
          />
          <br />
          健康な髪を守るためにも、地肌に直接アプローチします。<br />
          <br />
          ※頭以外の直接肌に触れる部分はタオルの上から施術します。
        </p>
      </div>
      <div class="grid_item six fadein">
        <h4>➅ 水やオイルを使いません</h4>
        <p>
          水やオイルを使わないドライマッサージです。お出掛けのご予定を気にせず、お気軽にお越しいただけます。
        </p>
      </div>
      <div class="grid_item img2 fadeinImg"></div>
    </div>
  </div>
</section>

<style>
  .features {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding-block: 40px;
  }

  .reason {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
  }

  h3 {
    font-size: 24px;
    font-weight: 700;
    color: #2e2e2e;
    border-bottom: 3px solid #ffb6c1;
    padding-bottom: 4px;
  }

  h4 {
    font-size: 20px;
    margin: 0 auto 16px 0;
  }

  p {
    color: #2e2e2e;
    font-size: 18px;
    letter-spacing: 0.15em;
    line-height: 175%;
  }

  a {
    color: inherit;
    text-decoration: none;
  }
  a:hover {
    opacity: 0.9;
  }

  button {
    font-size: 14px;
    margin: 16px auto 0px auto;
    padding: 4px 8px;
    border: none;
    background-color: #fafafa;
    border-bottom: 1px solid #2e2e2e;
  }

  /* gridレイアウト */
  .wrapper {
    margin-top: 2.4rem;
    display: grid;
    width: 896px;
    grid-template-columns: 1fr 1fr;
    grid-template-rows: repeat(15, 85px);
    gap: 24px;
  }

  .grid_item {
    display: flex;
    flex-direction: column;
    border: 1px solid #ececec;
    background-color: #fafafa;
    border-radius: 8px;
    padding: 16px;
    box-shadow: 2px 2px 6px 0px rgba(0, 0, 0, 0.2);
  }

  .one {
    grid-column: 1;
    grid-row: 1/3;
  }
  .two {
    grid-column: 2;
    grid-row: 1/4;
  }
  .three {
    grid-column: 1;
    grid-row: 3/6;
  }
  .four {
    grid-column: 2;
    grid-row: 4/10;
  }
  .img1 {
    grid-column: 1;
    grid-row: 6/9;
    background-image: url(/features-img1.JPG);
    opacity: 0.6;
    background-repeat: no-repeat;
    background-position: center;
    background-size: cover;
    min-height: 240px;
    box-shadow: none;
  }
  .five {
    grid-column: 1;
    grid-row: 9/15;
  }
  .six {
    grid-column: 2;
    grid-row: 10/12;
  }
  .img2 {
    grid-column: 2;
    grid-row: 12/15;
    background-image: url(/features-img2.jpg);
    opacity: 0.6;
    background-repeat: no-repeat;
    background-position: bottom center;
    background-size: cover;
    min-height: 240px;
    box-shadow: none;
  }

  @media screen and (width < 900px) {
    .features {
      margin-inline: 16px;
    }
    .wrapper {
      display: block;
      width: auto;
      height: auto;
    }
    .grid_item {
      margin-bottom: 24px;
    }
  }

    /* フェイドインアニメーション */
  .fadein {
    opacity: 0;
    transform: translateY(20px);
    transition: 2s;
  }
  .fadeinImg {
    opacity: 0;
    transform: translateY(20px);
    transition: 2s;
  }

</style>

<script>
  document.addEventListener("DOMContentLoaded", function () {
    class ScrollObserver {
      constructor(els, cb, options) {
        //監視対象のelementsを取得
        this.els = document.querySelectorAll(els);
        //オプションのデフォルト設定を定義
        const defaultOptions = {
          root: null,
          rootMargin: "0px",
          threshold: 0,
        };
        this.cb = cb;
        //デフォルト設定に、個別設定のオプションをマージする
        this.options = Object.assign(defaultOptions, options);
        //
        this._init();
      }
      _init() {
        const callback = (entries) => {
          entries.forEach((entry) => {
            //監視対象の要素が領域内に入った場合の処理
            if (entry.isIntersecting) {
              this.cb(entry.target, true); //true=isIntersectingがtrue
              //処理後に監視を切る処理
              observer.unobserve(entry.target);
            }
          });
        };
        const observer = new IntersectionObserver(
          callback.bind(this),
          this.options
        );
        //対象の要素elを監視
        this.els.forEach((el) => observer.observe(el));
      }
    }

    const fadeinImg = function (el, isIntersecting) {
      if (isIntersecting) {
        el.style.opacity = 0.6; //fadein2との違い
        el.style.transform = "none";
      }
    };
    const fadeinTriggerImg = new ScrollObserver(".fadeinImg", fadeinImg, {
      rootMargin: "-200px 0px",
      threshold: 0,
    });
  });

</script>
